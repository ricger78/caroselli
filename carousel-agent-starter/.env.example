# Copia questo file in .env e riempi le chiavi che usi.
# .env non va mai committato (è in .gitignore).

# Pexels — foto stock reali per le slide con immagine (bgImagePath/photoPath).
# Chiave gratuita su https://www.pexels.com/api/
PEXELS_API_KEY=
